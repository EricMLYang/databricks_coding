[tool.ruff]
line-length = 100
target-version = "py312"   # 對齊 DBR 17.3 的 Python
extend-exclude = ["scratch", "*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
